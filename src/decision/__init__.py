"""decision package — daily decision card skeleton (V1.5.0).

Integrates market_state / sentiment_cycle / sector_snapshot /
basic_decision_rules into a single :class:`DailyDecisionCard`. Strictly
read-only: no writes, no network, no auto-backfill.
"""